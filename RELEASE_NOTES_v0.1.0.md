# Daily-Speak v0.1.0

Release date: 2026-09-19

Daily-Speak 第一个公开测试版本，面向 Android 手机英语口语每日练习。该版本优先保证离线题库、每日练习、录音、反馈和复习闭环可以运行；音素级发音评测和云端服务联调仍属于后续迭代。

## 本版功能

- 今日、复习、我的三页 Material 3 底部导航。
- 本地原创题库，按日期、难度和话题稳定生成每日练习。
- Room 持久化题目、Attempt、Feedback 和收藏记录。
- MediaRecorder 本地录音，文件保存在应用私有目录。
- Android 13+ 系统 SpeechRecognizer 文件转写适配器；识别失败时明确提示，不伪造转写。
- 基于识别置信度、音量、停顿、语速和动态范围的粗略本地发音评分。
- 反馈页展示中文含义、参考回答、用户转写、评分和改善建议。
- 支持反馈卡片折叠、参考回答 TTS、重录、下一题和收藏。
- DeepSeek 动态出题和文本反馈入口；调用前需要用户确认，录音文件不上传。
- 复习页支持低分题、收藏题、历史记录以及日期、话题、难度筛选。
- 每日提醒、Android 13 通知权限和重启后恢复调度。

## APK

文件：`Daily-Speak-v0.1.0.apk`

包名：`com.thehan.dailyspeak`

版本：`versionCode 1` / `versionName 0.1.0`

支持：Android 8.0+（minSdk 26），目标 SDK 36

SHA-256：

```text
602414ABB2EDF13A2073AEA01961484A3405F4505373F72CB88FC70F22DD47E8
```

APK 使用仓库外生成并保存的 Release 密钥签名，仓库和 GitHub 不包含签名密钥或密码。

## 安装

1. 下载 `Daily-Speak-v0.1.0.apk`。
2. 在手机“设置”中允许当前浏览器或文件管理器安装未知来源应用。
3. 打开 APK 并按提示安装。
4. 首次录音时允许麦克风权限；Android 13+ 首次开启提醒时允许通知权限。
5. Android 13+ 文件转写还要求设备具有可用的系统语音识别服务。

## 已知限制

- 当前没有在红米 K60 真机上完成最终安装和性能验收，发布标记为测试预发布版本。
- Android `SpeechRecognizer` 对任意音频文件的转写能力依赖系统实现，部分设备可能不支持。
- 默认发音评分是本地粗略估算，不是音素级评测，不代表专业考试评分。
- DeepSeek 模型名、接口路径和请求格式需要根据官方文档和个人 API 账号完成真实联调。
- DeepSeek API Key 当前保存在本机 DataStore；正式生产版计划迁移到 Android Keystore。
- Room 当前使用 destructive migration fallback，正式发布前需要完成数据库迁移策略。
- 数据导出/清除、连续打卡、间隔重复和崩溃监控尚未完成。

## 隐私说明

- 录音默认只保存在应用私有目录，不上传。
- DeepSeek 仅接收用户确认后的文本内容，不接收录音文件。
- API Key、录音、转写和反馈不得提交到 Git 或写入普通日志。
